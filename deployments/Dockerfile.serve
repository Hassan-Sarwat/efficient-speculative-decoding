FROM vllm/vllm-openai:v0.6.4.post1

WORKDIR /app

# Only install EXTRA dependencies for your benchmarking (if missing)
# Combine unrelated RUNs to reduce layer count (minor optimization)
RUN pip install --no-cache-dir pandas scipy datasets

# Copy ONLY what is needed
COPY benchmark.py /app/

# Use the official entrypoint or your script
CMD ["python", "benchmark.py"]