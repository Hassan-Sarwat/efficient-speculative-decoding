# 1. Unsloth (Now compatible with Py3.11)
unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git

# 2. Key Libraries (Pin vLLM for stability)
vllm==0.6.4.post1
transformers==4.46.0

# 3. Remove the 'numpy<2.0.0' pin unless vLLM explicitly fails.
# PyTorch 2.4 often ships with newer numpy.
# If you get a numpy error later, re-add "numpy<2.0.0" here.

# 4. Standard Utils
datasets
pandas
wandb
scipy
trl
peft
accelerate